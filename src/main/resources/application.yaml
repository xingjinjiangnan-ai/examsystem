server:
  port: 8080

spring:
  application:
    name: examsystem
  datasource:
    url: jdbc:mysql://localhost:3306/examsys?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: examsys
    password: examsys123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        "[format_sql]": true
        "[dialect]": org.hibernate.dialect.MySQLDialect

  ai:
    openai:
      api-key: EMPTY
      base-url: http://100.64.0.1:1234
      chat:
        options:
          model: qwen-3.5-4b

sa-token:
  token-name: satoken
  timeout: 86400
  is-concurrent: false
  is-log: true